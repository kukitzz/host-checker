name: Release

on:
  push:
    tags: ["v*.*.*"]
  workflow_dispatch:

permissions:
  contents: write   # for creating GitHub Releases

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Build wheel + sdist
        run: |
          python -m pip install --upgrade pip build
          python -m build

      - name: Sanity-check the built wheel
        run: |
          python -m venv /tmp/relcheck
          /tmp/relcheck/bin/pip install dist/*.whl
          /tmp/relcheck/bin/hostchecker providers

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          files: dist/*
          generate_release_notes: true

# -----------------------------------------------------------------------------
# PyPI and Docker/GHCR publishing are intentionally disabled for now.
# To enable later:
#   1. Add a `pypi` environment in the repo settings and configure PyPI
#      trusted publishing for the `ioc-hostchecker` project.
#   2. Uncomment the `pypi` and `docker` jobs below and add them to the
#      release job's `needs:` if you want the GitHub Release gated on them.
#
#   pypi:
#     needs: release
#     runs-on: ubuntu-latest
#     environment: pypi
#     permissions:
#       id-token: write
#     steps:
#       - uses: actions/checkout@v4
#       - uses: actions/setup-python@v5
#         with:
#           python-version: "3.12"
#       - run: |
#           python -m pip install --upgrade pip build
#           python -m build
#       - uses: pypa/gh-action-pypi-publish@release/v1
#         with:
#           skip-existing: true
#
#   docker:
#     needs: release
#     runs-on: ubuntu-latest
#     permissions:
#       packages: write
#     steps:
#       - uses: actions/checkout@v4
#       - uses: docker/setup-qemu-action@v3
#       - uses: docker/setup-buildx-action@v3
#       - uses: docker/login-action@v3
#         with:
#           registry: ghcr.io
#           username: ${{ github.actor }}
#           password: ${{ secrets.GITHUB_TOKEN }}
#       - uses: docker/build-push-action@v6
#         with:
#           context: .
#           platforms: linux/amd64,linux/arm64
#           push: true
#           tags: |
#             ghcr.io/${{ github.repository }}:latest
#             ghcr.io/${{ github.repository }}:${{ github.ref_name }}
# -----------------------------------------------------------------------------
